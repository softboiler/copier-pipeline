"""Command-line interface."""

from cappa import invoke

from {{ package_name }}_pipeline.cli import Pipeline


def main():
    """CLI entry-point."""
    invoke(Pipeline)


if __name__ == "__main__":
    main()
