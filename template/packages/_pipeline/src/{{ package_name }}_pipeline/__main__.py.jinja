"""Command-line interface."""

from {{ package_name }}_pipeline.cli import Pipeline
from {{ package_name }}_pipeline.parser import invoke


def main():
    """CLI entry-point."""
    invoke(Pipeline)


if __name__ == "__main__":
    main()
