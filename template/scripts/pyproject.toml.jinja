[build-system]
requires = ["flit_core==3.9.0"]
build-backend = "flit_core.buildapi"

[project]
name = "{{ project_name }}_tools"
version = "0.0.0"
description = "Tools."
dependencies = ["cyclopts==2.5.0", "pipx==1.5.0"]
[project.scripts]
"{{ project_name }}_tools" = "{{ project_name }}_tools.__main__:main"

[tool.fawltydeps]
deps = ["scripts/pyproject.toml"]
code = ["scripts/{{ project_name }}_tools"]
ignore_undeclared = [
    "{{ project_name }}_docs",
    "{{ project_name }}_tests",
    "{{ project_name }}_tools",
    "{{ project_name }}",
]
ignore_unused = [
    # ? Core script tools
    "pipx", # For `pipx run` of scripts with dependencies isolated by PEP 723
]
