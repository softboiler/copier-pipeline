# This was automatically generated by a Copier template. It will only be created again if this file is deleted.
# See https://flit.readthedocs.io/en/latest/pyproject_toml.html for more fields.

[build-system]
requires = ["flit_core >=3.3,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "dummy"
readme = "README.md"
authors = [{name = "Blake Naccarato", email = "blake.naccarato+dev@gmail.com"}]
dynamic = ["version", "description"]

[project.urls]
Home = "https://github.com/blakeNaccarato/copier-python"
